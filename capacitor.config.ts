import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'guard_storage',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
